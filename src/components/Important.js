import React from 'react'

const Important = () => {
    return (
        <div>
            <h1>IMPORTANT!!!!</h1>
            <p>Due to some technical fault from the creators of the API the christmas category is not working (I have also checked from the original source and still same result). <br/>It will be there in case the developers fix that bug and you can use that feature too!</p>
        </div>
    )
}

export default Important